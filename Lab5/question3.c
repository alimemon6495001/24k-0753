#include <stdio.h>
int main(){
    int n;
    printf("number");
    scanf("%d",&n);
    if (n%3==0 && n%5==0)
    printf("number is divisible");
    else 
    printf("not divisible");
    
    return 0;
}